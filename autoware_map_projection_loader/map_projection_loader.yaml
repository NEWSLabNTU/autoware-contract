version: 1
nodes:
  map_projection_loader:
    pub:
      map_projector_info:
        required: true
    paths:
      startup:
        input: []
        output: [map_projector_info]
        max_latency_ms: 1000

topics:
  # relative — resolves via scope ns (e.g. /map/map_projector_info)
  map_projector_info:
    type: tier4_map_msgs/msg/MapProjectorInfo  # TODO verify
    pub: [map_projection_loader/map_projector_info]
